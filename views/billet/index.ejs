<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

  <title>Document</title>
</head>

<body>
  <%- include("../header") %>
  <main class="container">
    <h1>Billets</h1>

    <% billets.forEach( (billet) => { %>
    <ul>
      <li> Vol : <%= billet.volId._id %> </li>
      <li> Passager : <%= billet.passagerId._id %> </li>
      <li> Siège <%= billet.numeroSiege %> </li>
      <li> Classe : <%= billet.classe %> </li>
      <li> Date de reservation : <%= billet.dateReservation %> </li>
      <li> Status : <%= billet.statut %> </li>
    </ul>
    <form method="POST" action="/api/billet/<%= billet._id %>?_method=DELETE" class="d-inline">
      <button type="submit" class="btn btn-outline-primary">Delete</button>
    </form>
    <a href="/api/billet/<%= billet._id %>/update" class="btn btn-outline-primary">Update</a>
    <hr>
    <% }) %>


    <h2>Billets vendu ce mois-ci</h2>
    <h5>Total : <%= venteDuMois.length %></h5>
    <h2>Revenu Total des ventes</h2>
    <h5>Total : <%= CA %>€</h5>
    <h2>Classe le plus souvent acheter</h2>
    <h5>La classe la plus vendu est : <%= classeLaPlusVendue%> </h5>
    <h2>Billet annulé</h2>
    <h5>Total : <%= billetAnnulee.length %></h5>

  </main>
</body>

</html>